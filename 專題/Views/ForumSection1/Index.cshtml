@model IEnumerable<專題.Models.EFModels.ForumSection1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Member.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sectionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mainBoardAdministrator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mainTopicId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Member.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mainBoardAdministrator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mainTopicId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
