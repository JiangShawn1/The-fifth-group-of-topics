@model IEnumerable<專題.Models.EFModels.Forum_S1MainTopic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Member.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.boardId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.boardName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.boardAdministrator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Member.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.boardId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.boardName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.boardAdministrator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
